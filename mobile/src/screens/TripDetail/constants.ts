/** 共享标签常量（TripDetail 与 Share 共用，消除两处重复定义） */

export const GRID_GAP = 10;

export const SLOT_LABEL: Record<string, string> = {
  morning: "上午",
  afternoon: "下午",
  evening: "晚上",
};

export const TYPE_LABEL: Record<string, string> = {
  attraction: "景点",
  meal: "餐饮",
  hotel: "住宿",
  transport: "交通",
};
